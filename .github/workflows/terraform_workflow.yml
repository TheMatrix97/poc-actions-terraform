name: Github Actions Terraform
on: [push]
jobs:
  format_check:
    name: Terraform Validation Check
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Extract Backend Config
        run: |
          echo "BACKEND_CONFIG<<EOF" >> $GITHUB_ENV
          echo "$BACKEND_CONFIG" >> $GITHUB_ENV
          echo "EOF" >> $GITHUB_ENV
        env:
          BACKEND_CONFIG: ${{ vars.backend_file_contents }}

      - name: load backend configuration
        run: echo -e ${{ env.GITHUB_ENV }} > backend.conf && cat backend.conf

      - uses: hashicorp/setup-terraform@v3

      - name: Terraform fmt
        run: terraform fmt -check
        continue-on-error: true

      - name: Terraform Init
        run: terraform init -backend-config=backend.conf
        env:
          AWS_ACCESS_KEY_ID:  ${{ secrets.AWS_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY:  ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          AWS_SESSION_TOKEN:  ${{ secrets.AWS_SESSION_TOKEN }}

      - name: Terraform Validate
        run: terraform validate -no-color

      